import 'package:flutter/material.dart';

AppBar appBar() {
  return AppBar(
    title: const Text(
      "Shop", style: TextStyle(fontWeight: FontWeight.bold, fontSize: 25),),
    centerTitle: true,);
}